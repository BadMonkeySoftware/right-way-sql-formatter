-- Test: FUNCTION_KEYWORD tokens respect UppercaseKeywords setting
select cast(e.Salary as decimal(10,2))
    ,convert(varchar(50), e.HireDate)
    ,isnull(e.ManagerID, 0)
    ,db_name()
    ,coalesce(e.Notes, 'N/A')
    ,nullif(e.Status, 'Inactive')
    ,object_id('Employees')
    ,schema_name(e.SchemaID)
    ,iif(e.Active = 1, 'Yes', 'No')
from Employees e
