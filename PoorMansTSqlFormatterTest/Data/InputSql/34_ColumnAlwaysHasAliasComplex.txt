-- Test: columnAlwaysHasAlias with complex expressions and multiple SELECT blocks
SELECT c.CustomerID, c.Name, SUM(o.Amount) Total, AVG(o.Amount)
FROM Customers c
LEFT JOIN Orders o ON c.CustomerID = o.CustomerID
GROUP BY c.CustomerID, c.Name

SELECT 
    e.*,
    e.Salary * 1.15 Bonus,
    CASE WHEN e.Salary > 50000 THEN 'Senior' ELSE 'Junior' END Level
FROM Employees e
WHERE e.Active = 1
